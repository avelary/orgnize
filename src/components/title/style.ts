import styled from "styled-components";

export const Container = styled.div`
    height: 3.6rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: .5rem;

    h1 {
        font-size: 2.8rem;
        font-weight: 400;
    }

    img {
        display: block;
        width: 1.8rem;
    }
`